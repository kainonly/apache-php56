## npm

安装cnpm

``` shell
npm install cnpm -g
```

更改npm国内镜像源

``` shell
$ npm config set registry https://registry.npm.taobao.org
```

NRM源管理

```
npm install nrm -g
```

查看源

``` shell
nrm ls
```

指定源

``` shell
nrm use taobao
```